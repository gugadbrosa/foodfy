{% extends "layout.njk" %}


{%block content%}
        <div id="intro">
            <div class="intro-content">
                <h1>As melhores receitas</h1>
                <p>Aprenda a construir os melhores pratos com receitas criadas por profissionais do mundo inteito.</p>
            </div>
            
            <div class="intro-img">
                <img src="/assets/chef.png" alt="Chef">
            </div>
        </div>

        <div class="content">
            <div class="wrapper">
                <h2>Mais Acessadas</h2>
            </div>
                 
            <section class="cards">
                {% for recipe in recipes %}
                    {% if loop.index <= 6 %}
                        <div class="card">
                            <div class="card__image-container" >
                                <img src="{{recipe.image}}" alt="{{recipe.title}}">
                            </div>
                            <div class="card__content">
                                <h3>{{recipe.title}}</h3>
                            </div>
                            <div class="card__author">
                                <p>por {{recipe.author}}</p>
                            </div>
                        </div>
                    {% endif %}    
                {%endfor%}
            </section>


        </div>

{% endblock %}        
   